{% extends 'base.html.twig' %}

{% block title %}Modifier produit{% endblock %}

{% block body %}
<h1>Modifier produit</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('admin_produit_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
